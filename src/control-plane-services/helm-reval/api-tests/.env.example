# Copy to api-tests/.env. Read by the REST Client VS Code extension.

SERVICE_BASE_URL=http://localhost:8080

# Local dev: run `make mock-jwks` and paste /tmp/reval-test-token below.
BEARER_TOKEN=PASTE_FROM_/tmp/reval-test-token

# Remote / staging: real IdP-issued JWT with helmreval:* scopes.
# SERVICE_BASE_URL=https://reval.stg.nvcf.nvidia.com
# BEARER_TOKEN=...
